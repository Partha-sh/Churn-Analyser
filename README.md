# Churn-Analyser
End-to-end ML churn prediction system using Scikit-learn, Pandas, and FastAPI. Built preprocessing pipelines with encoding and scaling, trained Logistic Regression and Random Forest models, handled class imbalance, evaluated using precision/recall/F1-score, and deployed a real-time prediction API.
